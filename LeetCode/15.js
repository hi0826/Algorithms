/**
 * @param {number[]} nums
 * @return {number[][]}
 */
const threeSum = (nums) => {
    const answer = [];
    if(nums.length <= 1) return answer;

    nums.sort((a, b) => a-b);
    

};

threeSum([-1,0,1,2,-1,-4]);