/**
 * @param {number[]} nums
 * @return {number[][]}
 */
const threeSum = (nums) => {
    const answer = [];
    if(nums.length <= 1) return answer;

    nums.sort((a, b) => a-b);
    for(let i = 0 ; i < nums.length; ++i) {
        
    }

};

threeSum([-1,0,1,2,-1,-4]);