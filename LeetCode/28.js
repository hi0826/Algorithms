/**
 * @param {string} haystack
 * @param {string} needle
 * @return {number}
 */
const strStr = (haystack, needle) => {
    let answer = -1;
    answer = haystack.indexOf(needle);
    return answer;
};